# Limit Cycle Demo Session Configuration

session_name: "limit_cycle_demo"
user_name: "CLEF_Demo"
sample_data_dir: "./demo_output/limit_cycle"

session_parameters:
  num_samples: 2000
  save_samples: false
  save_metadata: true
