# Recording Playback Session Configuration

session_name: "recording_playback"
user_name: "CLEF_Demo"
sample_data_dir: "./demo_output/recording_playback"

session_parameters:
  num_samples: 1600
  save_samples: false
  save_metadata: true
