# CLEF Minimal Experiment Configuration
# Place this in: config/defaults/experiment_default.yaml

# Experiment metadata
experiment_name: "default_experiment"
experimenter: "Unknown"
output_dir: "./data"
save_images: true
save_metadata: true
save_sample_video: false # experiments may want to save a summary plot

# Acquisition parameters
acquisition:
  num_samples: 100
  frame_rate: null  # As fast as possible
  z_planes: 1

  # z_stack: false
  # z_start: 0.0
  # z_end: 0.0
  # z_step: 1.0
  # baseline_frames: 0
  # save_structural_scan: "none"

# Subject metadata (generic, domain-agnostic)
subject:
  subject_id: null
  subject_type: null
  subject_details:
    treatment: null
    treatment_details:
      condition: null
    orientation: null
  notes: null

# Z-step size (duplicated for backward compatibility)
# z_step_size_um: 1.0

# Input recording path
# input_recording_path: null

# Development options
dev_options:
  prefill_wb_ops: false
  send_sms_on_completion: false